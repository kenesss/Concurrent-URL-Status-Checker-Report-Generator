# Concurrent-URL-Status-Checker-Report-Generator
Build a command-line tool in Go that takes a list of URLs from a file, checks their HTTP status concurrently, and generates a summary report (with response time) in a separate file.
